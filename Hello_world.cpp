#include <iostream>
#include <vector>

int main()
{
    // Пользователь вводит с клавиатуры число n — размер вектора, который надо заполнять.
    std::string text = " ";
    std::cout << "Enter the text: Hello World: ";
    std::cin >> text;
    std::cout << text;
     std::cout << text;
      std::cout << text;
       std::cout << text;
        std::cout << text;
}